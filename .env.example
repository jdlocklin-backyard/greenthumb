# =============================================================================
# GreenThumb Platform - Environment Configuration (Simplified)
# =============================================================================
# Copy this to .env and customize values for your deployment
#
# QUICK START: The defaults work out of the box for development!
# Just run: docker compose up -d
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PostgreSQL + PostGIS)
# -----------------------------------------------------------------------------
# Change password for production!
POSTGRES_USER=greenthumb
POSTGRES_PASSWORD=greenthumb_dev
POSTGRES_DB=greenthumb

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------
# IMPORTANT: Generate a real secret for production:
# openssl rand -hex 32
SECRET_KEY=dev-secret-change-in-production-use-openssl-rand-hex-32

# -----------------------------------------------------------------------------
# Agent Configuration
# -----------------------------------------------------------------------------
AGENT_LOG_LEVEL=INFO

# Weather API (Open-Meteo is free, no key required)
WEATHER_API_PROVIDER=open-meteo

# Optional: OpenWeatherMap API key (if you prefer it over Open-Meteo)
# OPENWEATHERMAP_API_KEY=your_key_here

# -----------------------------------------------------------------------------
# Development Options
# -----------------------------------------------------------------------------
# Set to development for hot reload and debug logging
# NODE_ENV=development
